# Full stack: Postgres, Redis, API, worker and the Caddy-served frontend.
#   docker compose up -d --build
# Database migrations run automatically when the api container starts.

x-backend-env: &backend-env
  DATABASE_URL: postgresql+asyncpg://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@db:5432/${POSTGRES_DB:-ai_debates}
  REDIS_URL: redis://redis:6379/0
  MEDIA_ROOT: /media

services:
  db:
    # Keep the major version: upgrading requires migrating the data volume (pg_upgrade).
    image: postgres:15-alpine
    restart: always
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-ai_debates}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-ai_debates}"]
      interval: 5s
      timeout: 5s
      retries: 12

  redis:
    image: redis:7-alpine
    restart: always
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 12

  api:
    build:
      context: ./backend
      target: api
    restart: always
    command: >
      sh -c "alembic upgrade head &&
             uvicorn app.main:app --host 0.0.0.0 --port 8000 --proxy-headers --forwarded-allow-ips='*'"
    env_file:
      - .env
    environment:
      <<: *backend-env
    volumes:
      - media_data:/media
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health', timeout=3)"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s

  # Debate turns (LLM streaming).
  worker:
    build:
      context: ./backend
      target: worker
    restart: always
    command: python -m app.worker --queues default
    env_file:
      - .env
    environment:
      <<: *backend-env
    volumes:
      - media_data:/media
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  # Audio builds (TTS + ffmpeg). Separate process so a long build never blocks debate turns.
  media-worker:
    build:
      context: ./backend
      target: worker
    restart: always
    command: python -m app.worker --queues media
    env_file:
      - .env
    environment:
      <<: *backend-env
    volumes:
      - media_data:/media
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  frontend:
    container_name: ai-debates-frontend-1
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=/api
    restart: always
    ports:
      - "80:80"
    environment:
      # Plain HTTP; TLS is terminated by the external Caddy in production (see docker-compose.prod.yml)
      - DOMAIN_NAME=:80
    depends_on:
      - api

volumes:
  postgres_data:
  media_data:
